// Create function that takes in one argument that is passed through a conditional statement with if, else ifs, and else. Return the result as an alert or console.log. Test all of your conditions to make sure you can receive all of your results back. 

function mysteryAnimal(num) {
	if (num==1) {
		alert ("cat");
	} else if (num==2) {
		alert ("dog");
	} else if (num==3) {
		alert ("bunny");
	} else if (num==4) {
		alert ("mini pig");
	} else {
		alert ("unicorn :0");
	}
}